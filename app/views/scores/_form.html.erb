<div class="col-4 offset-4">

  <%= form_with(model: score, local: true) do |form| %>
    <% if score.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(score.errors.count, "error") %> prohibited this score from being saved:</h2>

        <ul>
        <% score.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :wine_id, class:"control-label" %>
      <%= form.text_field :wine_id, class:"form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :enologist_id, class:"control-label" %>
      <%= form.text_field :enologist_id, class:"form-control" %>
    </div>

    <div class="form-group">
      <%= form.label :score, class:"control-label" %>
      <%= form.number_field :score, class:"form-control" %>
    </div>

    <div class="actions">
      <%= form.submit "Save", class:"btn btn-success" %>
    </div>
  <% end %>

</div>